[DONE] EMAIL AUTHORS
prune word vocab 
threshold for cue words
prune char vocab (non hindi chars, blows vocab size)

TODO
[done] send email to everyone
[done] sentence wise ppl (batch wise, print sentences and predicted)
trigram clusters (overlapping) [later = lat + ate + ter]
bigram clusters (overlapping)
[done] frequency dist for each independently and then determine threshold (separately)

MODIFICATIONS
[done] use tokens only from training data
word vector generation from subword units: sum, conv filters
sparse softmax vs softmax compare (per batch)

NEXT MEETING Tuesday 4PM
attention (replace max pool over time)